@model IEnumerable<Hld.WebApplication.ViewModel.CatageorySub3ViewModel>


<table id="categorySub3" class="table table-hover">
    <thead>
        <tr>
            @*<th>
            <label id="label">Sr #</label>
        </th>*@
            <th>
                <label id="label">Category 4</label>
            </th>
            <th>
                <label id="label">sku's</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.HiddenFor(e => item.CatageorySub2Id)
                    @Html.HiddenFor(e => item.CatageorySub3Id)
                    
                    @Html.DisplayFor(modelItem => item.CatageorySub3Name)
                </td>
                <td>0</td>
                <td>
                    @if (item.status == "not-exists")
                    {
                        <a asp-controller="Catageory" asp-action="DeleteCategorySub3" asp-route-id="@item.CatageorySub3Id" data-Url="/Catageory/CategoriesListSub4/@item.CatageorySub3Id" class="deleteAble">Delete</a>
                    }
                    else
                    {
                        <a asp-controller="Catageory" asp-action="DeleteCategorySub3" asp-route-id="@item.CatageorySub3Id" data-Url="/Catageory/CategoriesListSub4/@item.CatageorySub3Id" style="pointer-events: none;color:lightgrey" class="not-deleteAble">Delete</a>
                    }

                </td>
            </tr>
        }
    </tbody>
</table>
